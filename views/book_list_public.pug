extends layout_public

block content
  h1= title

  ul
    each book in book_list
      li
        a(href=book.public_url) #{book.title}
        |  (#{book.author.name})

    else
      li There are no books.